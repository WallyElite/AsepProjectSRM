PID config b stabilizes the fastest, seen by its high kp value and relative kd and kd values. Config c is overdamped and it turn oscillates from overdampening and coming short and overshooting. If Kd was set to 0, the PID control would keep overshooting from kp depending on high kp is just like kd except it would start with an overshoot if too high. The risk of setting Ki too high would accumulate error quicker, meaning that the entire controller would overshoot from trying to correct such a huge collection of error.